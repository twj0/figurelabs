# FigureLabs AI — Environment Configuration
# Copy this file to .env and adjust values

# Server
PORT=11451

# Database path (relative to project root, or absolute)
DB_PATH=./data/figurelabs.db

# Optional API key for OpenAI-compatible /v1 endpoints
OPENAI_API_KEY=
